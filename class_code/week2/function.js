function sayhi(name) {
  console.log(`hello ${name}`);
}

const sayhi2 = (name) => {
  console.log(`howdy ${name}`);
};

const sayhi3 = (name) => {
  return `suppppppp ${name}`;
};

MediaSourceHandle.exports = { sayhi1, sayhi2, sayhi3 };
